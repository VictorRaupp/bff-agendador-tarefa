spring.application.name=bff-agendador-tarefa

usuario.url=${USUARIO_URL:http://localhost:8080/usuario}
agendador-tarefa.url=${AGENDADOR_TAREFA_URL:http://localhost:8081/tarefa}
notificacao.url=${NOTIFICACAO_URL:http://localhost:8082/email}

server.port=${SERVER_PORT:8084}

cron.horario=${CRON_HORARIO:0 */5 * * * *}

usuario.email=${USUARIO_EMAIL}
usuario.senha=${USUARIO_SENHA}